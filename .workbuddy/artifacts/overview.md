# 功能入口迁移 + 圈子角标 — 完成概览

## 改动文件
- `src/views/profile/MyProfile.vue` — 功能入口 HTML 位移 + CSS 重写
- `src/views/message/MessagePage.vue` — 圈子角标："群"→"圈"

## 变化对比

### 1. 功能入口位置迁移（MyProfile）

```
之前（卡片外独立区块）：              之后（卡片内部标签行下）：
┌────────────────────┐              ┌────────────────────┐
│ [头像] 林小北 ♂     │              │ [头像] 林小北 ♂     │
│ 校声号：echo...    │              │ 校声号：echo...    │
│ 学校：中央民族大学   │              │ 学校：中央民族大学   │
│ ─────────────────  │              │ ─────────────────  │
│ [20岁] [摄影] [日语]│              │ [20岁] [摄影] [日语]│
├────────────────────┤              │ ┌──────┬──────┬──────┐│
│ 🕐浏览 │ 📋草稿 │ 👥圈子 │              │ │🕐 浏览│📋草稿│👥 圈子 ││
├────────────────────┤              │ └──────┴──────┴──────┘│
│ [搜索框...]         │              └────────────────────┘
└────────────────────┘              ┌────────────────────┐
                                    │ 帖子 评论 收藏 赞过 🔍 │
                                    └────────────────────┘
```

| 项目 | 之前 | 之后 |
|------|------|------|
| 位置 | `.user-card` 外部独立区块 | `.user-card` 内部 `.tags-row` 正下方 |
| 样式 | 整条灰底 `var(--echo-bg)` + `border-left` 竖线分隔 | 三个独立 `#f5f6f8` 浅灰圆角方框 |
| 间距 | `gap: 0` (靠 border-left) | `gap: 8px` |
| 图标 | 18px | 16px |
| 圆角 | 10px (容器) | 10px (每个方框) |

### 2. 圈子角标文字

仅一行文字改动，`MessagePage.vue:115`：
```
- <div v-if="chat.isGroup" class="group-badge">群</div>
+ <div v-if="chat.isGroup" class="group-badge">圈</div>
```
样式零改动：绿色 `--echo-primary` 背景、白色文字、9px 字号、6px 圆角。
